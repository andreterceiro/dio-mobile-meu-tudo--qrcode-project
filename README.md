# DIO project

Please remember to use a Node.js version that supports the parameter '--env-file'. I used the Node.js version 20. I installed this version with the command:

```
nvm install 20
```
 And used this Node.js version with the command:

 ```
 nvm use 20
 ```

[Installation of NVM](https://blog.formacao.dev/nvm-instalacao-e-utilizacao/) (in portuguese)